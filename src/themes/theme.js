import { createMuiTheme } from "@material-ui/core";

export const theme = createMuiTheme({
  palette: {
    background: {
      default: "#fff",
    },
  },
  primaryDark: "#0D0C1D",
  primaryLight: "#EFFFFA",
  primaryHover: "#343078",
  mobile: "576px",
});
